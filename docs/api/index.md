# API reference

The API reference is organized by major public modules, including the V3 `audit` and `subgroup` surfaces, the V4 `selective` surface, and the V5 `bench` and `experiments` surfaces for reproducible scientific benchmarking.
